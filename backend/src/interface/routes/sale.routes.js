// sale.routes.js
// Capa de interfaz: Rutas HTTP de Venta

import { Router } from 'express';
// TODO: Importar controlador
// import SaleController from '../controllers/sale.controller.js';

const router = Router();

// TODO: Definir rutas
// router.post('/', SaleController.create);
// router.get('/', SaleController.getAll);
// router.get('/:id', SaleController.getById);
// router.get('/user/:userId', SaleController.getByUser);
// router.get('/customer/:customerId', SaleController.getByCustomer);
// router.get('/date-range', SaleController.getByDateRange);
// router.patch('/:id/cancel', SaleController.cancel);

export default router;
